'use strict'

class Console {
  static info (descricao, item) {
    console.info(descricao, item)
  }

  static payload (descricao, item) {
    console.info(descricao, item)
  }

  static error (descricao, item) {
    console.info(descricao, item)
  }
}

module.exports.Console = Console
